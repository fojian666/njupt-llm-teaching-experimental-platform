# 物联网学科大模型教学实验平台 — 项目理解与建设方案

> 编写日期：2026-09-12　状态：待确认
> 依据：工作区现有 7 份资料 + 演示视频逐帧分析（17 帧）

---

## 一、资料盘点

| 文件 | 类型 | 在本项目中的角色 |
|---|---|---|
| `0.1-物联网学院简介-2025.8.17(2).docx` | 学院介绍（中英） | 语料 |
| `1-2025级-网络工程专业培养方案.docx` | 培养方案 | 语料 |
| `3-2025级-物联网工程专业培养方案20250813.docx` | 培养方案 | 语料 |
| `5-地理信息科学专业培养方案.doc` | 培养方案 | 语料 |
| `物联网工程导论 第2版 (吴功宜).pdf` | 教材（扫描件） | 语料，**已 OCR** |
| `物联网概论(第3版) (韩毅刚).epub` | 教材 | 语料，文字层完整、可直接解析 |
| `演示视频.mp4` | 平台演示（2′12″，无配音） | **目标形态参照** |

结论：现有资料 = **待入库的语料** + **目标产品的功能参照**，两者都是输入，没有现成代码。

---

## 二、演示视频拆解（目标平台长什么样）

### 2.1 整体框架
顶部标题「行业大模型管理平台」，左侧 4 个一级模块：

```
数据管理 ── 数据分类管理
        └─ 知识管理
智能体中心 ─ 智能体广场
配置中心
问答记录
```

### 2.2 出现的页面与功能点

| # | 页面 | 观察到的功能元素 |
|---|---|---|
| 1 | 数据分类管理 | 左侧**分类树**（调查监测 / 空间规划 / 01法律法规 / 03规范性文件 / 04标准规范 / 05办事指南 / 用途管制 / 开发利用 / 保护修复 / 确权登记 / 自然资源调查 / 资产清查 / 防灾减灾 / 综合支撑 / 不动产登记→01法律·02案例·03司法解释·04政策文件·05标准规范·06通知公告…）；右侧**数据表**（数据名称、数据分类、上传方式、数据格式、数据标签、解析状态、上传时间；操作：详情 / 查看源文件 / 修改数据信息 / 删除）；工具栏 `添加标签 / 移动分类 / 删除 / 刷新 / 批量重新解析`；筛选区 `数据名称 / 上传方式 / 数据格式 / 数据标签 / 上传时间区间`；共 36 条 |
| 2 | 数据详情 | 原文渲染（保留标题层级，如"第三节 调研地区现状分析"）+ `下载数据` |
| 3 | 知识库详情 | 表：数据名称 / 数据格式 / **解析状态** / 上传时间 / **是否有效（开关）**；操作：切片详情 / 重新切片 / 查看数据 / 删除；按钮 `+导入知识 / 删除 / 重新切片 / 刷新`；筛选 `生效状态 / 数据格式 / 解析状态 / 数据名称`；共 4 条 |
| 4 | 导入知识 | 筛选 → 候选数据列表 + `加入已选列表` → 底部**已选列表** → `确认导入`（导入成功 toast）；候选共 251 条 |
| 5 | 切片列表 | 切片编号 / 内容 / 操作（详情、删除）；分页共 113 条 |
| 6 | 切片详情 | `切片编号` + `内容`（**文件名 + 章节路径 + 正文片段**），可编辑保存 |
| 7 | 智能体体验页 | 顶栏 `详情 / 体验 / 调用` 三 Tab；左侧会话历史（今天 / 近七天）+ `新对话` + 关键词搜索；主区欢迎语"您好，我是自然资源智能管家"+ 推荐问题（可"换一批"）；底部**模型选择器（DeepSeek-V3 / Qwen3-30B）+ 知识库选择器（不动产登记知识库 / 测试3 / 测试3 13）** |
| 8 | 问答结果 | 结构化长答（一、问题概述　二、核心回答　三、补充建议　四、结论）+ **行内引用角标 [1]–[5]** + 右侧**「检索来源」面板**（列出命中切片及其章节路径）+ 底部 `检索优先` 开关 + 复制 / 赞 / 踩 / 重新生成 |

> 视频中 **未演示** `配置中心` 与 `问答记录` 两个模块，仅有导航项——这两块需要另行确认需求。

### 2.3 平台本质
一个**面向垂类行业的 RAG 知识库问答平台**（演示行业=自然资源/不动产）：

```
文档上传 → 解析(docx/pdf/xlsx) → 数据分类打标 → 沉淀进知识库
        → 切片(chunk) → 向量化 → 检索增强问答(RAG) → 带引用来源的回答 → 问答日志
```

### 2.4 推断：本项目要做的是"换皮换料"
把上面这套平台的**领域从「自然资源」换成「物联网学科教学」**：

| 演示平台 | 本项目应为 |
|---|---|
| 自然资源法规/研究报告 | 物联网教材 + 培养方案 + 课程资料 |
| 自然资源智能管家 | 物联网学科智能助教 / 培养方案问答 / 课程知识点问答 |
| 自然资源分类树 | 学科知识分类（按课程 / 章节 / 专业） |
| 不动产登记知识库 | 物联网工程导论 / 物联网概论 / 培养方案 知识库 |

---

## 三、OCR 已完成

`ocr/物联网工程导论第2版/` 下已产出全书文本，明细见该目录 `README.md`：

- 338 页全部识别成功，**无空白页**，共 354,689 字
- 平均置信度 0.788，耗时 154 秒（macOS Vision 框架，8 线程）
- 逐页 `page-NNNN.txt` + 合并 `物联网工程导论-第2版-全文.txt`
- 已知问题：目录页双栏串栏、表格拍平、插图无描述

---

## 四、已锁定的决策（2026-09-12 确认）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 建设路线 | **自研**（不基于开源 RAG 平台二开） |
| 2 | 首期范围 | **四模块齐做** |
| 3 | 模型接入 | **纯云端 API**（OpenAI 兼容协议，DeepSeek / 通义均可） |
| 4 | 交付形态 | **能真跑的全栈系统** |

## 五、技术架构

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Vue 3 + TypeScript + Vite + Element Plus + Pinia | 中后台为主，Element Plus 覆盖表格/筛选/树/表单；SSE 用 `@microsoft/fetch-event-source` |
| API 层 | Django 5.2 LTS + Django Ninja | 管理端 CRUD 与权限靠 Django；Ninja 提供 Pydantic 类型 + 自动 OpenAPI（对应演示里的「调用」Tab） |
| 异步任务 | Celery + Redis | 文档解析、切片、向量化、批量重新解析 |
| RAG 服务层 | 独立 service 层（`backend/rag/`） | 不依赖 ORM / request，将来要拆成 FastAPI 服务只需搬文件 |
| 存储 | PostgreSQL 16 + pgvector（HNSW / cosine） | 一个库同时解决关系数据与向量检索；对象文件走本地 `MEDIA_ROOT`（无 Docker，暂不引 MinIO） |

### 数据模型（对应演示视频的四大模块）

```
accounts   User(role: admin/teacher/student)
configs    ModelProvider · ModelConfig(kind: llm/embedding/rerank) · SystemConfig
datasets   DataCategory(分类树) · DataTag · DataResource(解析状态/正文/标签)
knowledge  KnowledgeBase · KnowledgeDoc(是否有效) · Chunk(章节路径 + embedding 向量 + HNSW)
agents     Agent(模型/知识库/检索优先) · Conversation · Message(citations 引用来源)
audit      MessageFeedback(赞踩) · OperationLog(操作留痕)
```

## 六、推进顺序与当前进度

- [x] **语料工程（OCR）** — 《物联网工程导论》338 页全书 OCR 完成
- [x] **工程骨架 + 数据模型** — Django 工程、19 张表、pgvector 向量列与 HNSW 索引就绪
- [x] **RAG 服务层**（`backend/rag/`）— 解析器 7 种格式 / 切片器 / providers（OpenAI 兼容）/ 混合检索（pgvector 向量 + BM25 关键词加权融合）/ 生成器（流式 + 引用编号抽取）/ 入库流水线（断点续跑）
- [x] **Ninja API** — 数据管理、知识管理、智能体中心、配置中心、问答记录 + SSE 流式问答（事件：start / retrieval / citations / reasoning / delta / done / error）
- [x] **演示数据与端到端联调** — `python manage.py seed_demo` 一键灌入 7 份真实资料（1783 切片），鉴权 / 分类树 / 检索 / 流式问答 / 问答记录全部实测通过
- [ ] 前端骨架（Vue 3 + Vite + Element Plus + Pinia）— `frontend/package.json` 已备好，**依赖安装被沙箱拦截，需手动 `npm install` 放行**
- [ ] 四个模块的前端页面

## 七、如何跑起来

```bash
cd backend
# 1) 环境（已建好 .venv，数据库 iot_edu_platform 已启用 pgvector）
source .venv/bin/activate

# 2) 配置模型 Key（backend/.env）
#    LLM_API_KEY=...        默认 DeepSeek
#    EMBEDDING_API_KEY=...  默认通义 text-embedding-v3（1024 维已与库对齐）

# 3) 灌演示数据（--reset 会清空业务数据重灌；没配 Key 时加 --skip-index）
python manage.py seed_demo --reset

# 4) 起服务
python -m uvicorn config.asgi:application --host 127.0.0.1 --port 8000
#    OpenAPI 文档: http://127.0.0.1:8000/api/docs

# 5) 起前端（待依赖装好）
cd ../frontend && npm install && npm run dev
```

账号：`admin / admin123456`（管理员）、`teacher / teacher123456`、`student / student123456`。

**没配 Key 也能跑**：问答会走降级路径 —— 向量召回失败时自动退化为关键词检索，
模型不可用时返回可读的提示语并保留来源面板；「重新解析」可随时补跑向量化（断点续跑）。
