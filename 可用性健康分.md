# 可用性健康分（Nielsen 十条启发式评估）

评估对象：平台前端。评分标准：每条 0–5 分，本文记录的是一轮评估结果与整改状态。

## 评估结果与整改

| # | 启发式 | 首评 | 关键问题 | 整改状态 |
|---|---|---|---|---|
| 1 | 状态可见性 | 3 | 流式光标、停止生成到位；部分异步操作只有一次性 toast，按钮无等待态 | 已部分整改：数据「解析」按钮增加行内 loading |
| 2 | 系统与现实匹配 | 3 | RAG / LLM 用词自洽；用户消息直接渲染文本符合预期 | 保持 |
| 3 | 用户控制与自由 | 3 | 删除均有确认；流式可停止；无撤销（Undo） | 维持现状，Undo 暂不做 |
| 4 | 一致性与标准 | 2 | 双设计语言混搭；错误提示分支不一致 | 待整改：错误提示统一走拦截器 |
| 5 | 错误预防 | 3 | 内联校验、防重复提交到位；仍有少量静默 catch | 待排查：静默 catch 逐个标注 |
| 6 | 识别优于回忆 | 3 | 菜单带图标、tree-select、筛选下拉 | 保持 |
| 7 | 灵活性与效率 | 2 | Enter 发送、批量删除、JSON 导出齐全；但 pageSize 锁死 20、会话无深链 | 已整改：分页大小可选（10/20/50/100）；会话写入 URL，支持 ?c= 深链 |
| 8 | 美学与极简 | 3 | 设计令牌、tabular-nums、留白克制；登录动画略过 | 保持 |
| 9 | 错误识别与恢复 | 2 | 多处静默 catch；回答出错后无重试 | 已整改：出错回答增加「重试」按钮 |
| 10 | 帮助与文档 | 2 | 无帮助页；α / top_k / 检索策略等行话无解释 | 已整改：参数面板与检索调试页增加说明文字 |

首评合计 26/40（Acceptable）。本轮整改后预估 29–30/40，
剩余扣分点集中在错误提示统一性（#4、#5 的静默 catch）与无独立帮助页（#10）。

## 本轮整改明细

1. **分页大小可选**：问答记录、数据资源、知识文档三个列表的分页器加 sizes 选项，
   `@size-change` 时回到第 1 页重新加载。
2. **会话深链**：打开会话时把会话 id 写入 URL（`/agents/6/chat?c=12`），
   刷新或分享链接后直接恢复该会话；删除当前会话或新建会话时同步更新。
3. **出错回答可重试**：回答出错时在操作栏出现「重试」，按上一条用户提问重新生成；
   出错消息仅在前端摘除，服务端记录保留（刷新后历史里仍可见）。
4. **参数行话说明**：对话参数面板底部固定三条说明（top_k、检索优先、关键词召回）；
   检索调试页增加 α 含义与两路召回适用场景的说明。
5. **解析按钮等待态**：数据资源列表的「解析」按钮执行期间显示 loading，不再只是 toast。

## 待办（下一轮）

- 错误提示统一：所有 `catch {}` 逐个标注「已由拦截器提示」或补充提示，
  消除同一类错误有的弹 toast 有的静默的不一致。
- 帮助页：一个轻量的使用说明页，覆盖检索参数、切片策略、角色权限差异。
- 解析 / 导入 / 重新向量化等长任务的进度条（目前是轮询文本状态）。
