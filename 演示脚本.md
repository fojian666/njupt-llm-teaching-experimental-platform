# 7 分钟演示脚本

> 面向「物联网学科大模型教学实验平台」的演示录像。总时长 7:00，分镜按秒表排。
> 原则：**不按菜单顺序点**，用「教师建库 → 学生提问 → 管理员治理」一条故事线串起六个页面；
> 讲**过程与数字**，不念界面上的字。

---

## 0. 开录前检查清单（约 5 分钟）

- [ ] 起服务：`./start.sh`；再用 `./start.sh status` 确认 `后端(8000) 运行中`、`前端(5173) 运行中`
- [ ] 浏览器打开 `http://127.0.0.1:5173`，登录 `admin / admin123`
- [ ] **先随便问一个问题热身**：首次调用模型要走网络，别让第一个镜头卡在"正在生成"
- [ ] 分辨率 ≥ 1440×900，浏览器缩放 100%，收起书签栏，关闭通知与其他标签页
- [ ] 要问的问题**全部预置成智能体的推荐问题**，录制时只点按钮，不打字
- [ ] 关掉无关终端窗口（尤其别让后端日志刷屏跑到画面里）
- [ ] 备用方案：DeepSeek 抖动时改问更短的问题，或在「配置中心」切到备用 key

现成数据（可直接引用）：**11 条数据资源 · 10 个分类 · 2 个知识库 · 1816 条切片 · 2 个智能体**。

---

## 1. 分镜表

| 时间 | 画面 | 操作 | 讲什么（要点） |
|---|---|---|---|
| 0:00–0:35 | 登录页 → 智能体广场 | 登录后停在广场 | 问题背景 + 定位："把散落的学科资料变成可溯源问答" |
| 0:35–2:05 | 数据资源 → 数据分类管理 | 展示列表 → 上传小文件 → 解析 → 详情 → 分类树 | 家底清点 + 全链路走一遍；重点讲 OCR |
| 2:05–3:35 | 知识管理 | 切换知识库 → 文档列表 → 切片预览 → 检索调试 | 章节级切片（不是定长切）+ 向量化 + 两路召回 |
| 3:35–5:15 | 对话页（教材智能体） | 点推荐问题 → 看流式 → 讲检索条 → 讲引用面板 → 反馈 | 核心链路：检索过程可见、引用可溯源 |
| 5:15–6:05 | 对话页（教务智能体） | 切到第二个智能体问同一类问题 | 知识域隔离：同一批数据、不同知识库 → 不同答案 |
| 6:05–6:45 | 问答记录 → 配置中心 | 三个标签页 + 模型体检 | 治理与运维：统计、日志、模型可切换 |
| 6:45–7:00 | 架构图（可放 PPT 一页） | 静态 | 架构与取舍 + 下一步 |

---

## 2. 逐段话术（可直接照读）

### 0:00–0:35　开场

> 各位老师好，这是我们为物联网学科做的教学问答平台。
> 起因很实际：物联网专业的资料散在两本教材、三份培养方案和一堆学院文件里——有扫描版 PDF，有 epub，有 docx。
> 学生问的是"第 1 章 1.3 节讲的什么"，而搜索引擎给不了这种粒度，翻文档又太慢。
> 我们要做的是：**把学科资料变成能溯源到章节的问答系统**。整个平台分四块——数据管理、智能体中心、知识管理、配置与治理。

### 0:35–2:05　数据管理：从原始文件到可检索语料

> 这是数据资源列表，现在有 11 条：两本教材、三份培养方案、学院简介，以及平台自带的四篇原创示例语料。
> 我先现场传一份新文件走一遍流程。（**点右上「上传数据」，选一个小 txt**）
> 上传后点「解析」，它抽取正文并统计字符数；点「详情」可以看到解析出来的正文预览，以及它进了哪些知识库。
> 这里有两条数据值得说：这条 **338 页的《物联网工程导论》是扫描版**，没有文字层，直接解析只有零字符。
> 我们**用 macOS 的 Vision 框架自己写了 OCR 流程**，得到 35.4 万字，平均置信度 0.788，全过程 154 秒。
> 它的上传方式标着「OCR 导入」——平台里上传方式是数据来源的一部分，不是随便填的字段。
> （**切「数据分类管理」**）分类是树形的：教材、培养方案、学院概况、原创语料。分类会一路带到切片上，
> 所以后面检索来源里能看到每段话属于哪一章节、哪个专业。

### 2:05–3:35　知识管理：切片与向量化

> （**进「知识管理」**）知识库是"智能体能查什么"的边界。现在有两个：教学知识库装教材和示例语料，教务知识库只装培养方案和学院简介。
> 这是文档列表，点开任一条看「切片」。（**点切片**）
> 关键点在这里：**切片是按教材章节结构切的，不是按固定长度硬切的**。所以每一条都带着完整的章节路径，
> 比如"第 1 章 物联网概论 > 1.4 物联网体系结构 > 1.4.2 人处理物理世界问题的基本方法"。
> 这直接决定了回答能不能溯源——用户看到的不是"某本书第 12 段"，而是"某一节的哪一段"。
> 目前 **1816 条切片全部完成向量化**，用 BGE 模型，1024 维，存在 PostgreSQL 的 pgvector 里，走 HNSW 索引。
> （**滚到「检索调试」**）这一块是纯检索、不生成，用来解释"为什么这几段被选中"。
> 这里输入问题，下面会显示**向量候选多少条、关键词候选多少条**，以及每条的综合分、向量分、关键词分。
> 融合方式是加权：α 给向量、1-α 给关键词，这里可以现场调。

### 3:35–5:15　核心问答：可解释 + 可溯源

> （**切「智能体广场」，点"物联网学科智能问答"→「开始对话」**）回到学生视角。
> 我问一个教材问题。（**点推荐问题"物联网的体系结构分为哪几层？"**）
> 注意三处：
> 第一，**回答上方这行检索过程**——向量召回多少条、关键词召回多少条、融合后取前几条送进提示词。检索不是黑盒。
> 第二，**右侧来源面板**：5 条依据，每条都有章节路径、原文片段，以及三个分数——综合分、向量分、关键词分。
> 第三，回答里的 **[1][2] 编号与来源面板一一对应**，被真正引用的条目会标绿「已引用」。
> 这样做是有意的：**来源列了 5 条、回答其实只用 1 条，如果看起来一样可信，那引用就是装饰**。
> 所以只把用到的标为已引用，其余列出来备查。
> 另外可以看下最下方的耗时与模型，以及点赞点踩——这些会进入问答记录，形成治理闭环。
> （**再问一个教材没覆盖的问题**）问一个资料里没有的：它应该明确说明现有资料未涵盖，而不是凭常识编一段。
> 这是 RAG 里最容易被忽略、却最影响可信度的一条。

### 5:15–6:05　换个智能体：知识域隔离

> （**返回广场，进「培养方案与教务问答」**）同一个平台、同一批数据，换个智能体结果就不同——
> 这个智能体只挂了"培养方案与学院规章"知识库，系统提示词也换成了教务口径。
> 问同一个专业方向的问题。（**点推荐问题"物联网工程专业的毕业要求有哪些？"**）
> 可以看到**引用来源全部来自培养方案那一份文档**，教材里的内容不会串进来。
> 这就是"智能体 = 模型 + 提示词 + 知识域"的落地：不是所有问题都适合查全部资料。

### 6:05–6:45　治理与运维

> （**进「问答记录」**）三个标签页：问答记录里有每次提问的引用命中数、耗时、是否报错、用户反馈；
> 操作日志记录谁改了什么；使用统计给出问答量、满意度、平均耗时、按智能体的分布。
> （**进「配置中心」**）这里是模型接入：供应商、模型、以及一次"体检"——大模型和向量模型各自是否可用、Key 来自配置中心还是环境变量。
> 模型是可以在页面上切换的，问答时也能在参数面板里临时改；**接口全部走 OpenAI 兼容协议**，换供应商不用改代码。

### 6:45–7:00　收尾：架构与取舍

> （**PPT 一页架构图**）整体两条链路：离线入库是"解析 → 章节切片 → 向量化"；在线是"混合检索 → 加权融合 → 编号提示词 → 流式生成 → 引用抽取"。
> 三个取舍说明一下：**没有用 LangChain 这类框架**，检索、生成、切片都是自己写的 service 层，逻辑完全可控、便于解释与替换；
> **向量和业务数据放在同一个 PostgreSQL 里**，少一套组件，运维简单；
> **引用只标真正用到的**，宁可少标也不虚标。
> 下一步是给检索加交叉编码器重排，并扩充评测集。谢谢各位老师。

---

## 3. 预置问题清单（照抄进智能体的「推荐问题」）

教材智能体（物联网学科智能问答）：

1. 物联网的体系结构分为哪几层？
2. RFID 系统由哪几部分组成？
3. ZigBee 技术有什么特点？
4. 传感器网络有哪些特点？
5. 物联网面临哪些安全威胁？
6. 组织一次学校图书馆的借阅流程是怎样的？（**用于演示"资料未覆盖"时的处理**）

教务智能体（培养方案与教务问答）：

1. 物联网工程专业的毕业要求有哪些？
2. 网络工程专业的核心课程包括哪些？
3. 毕业学分要求是多少？
4. 学院有哪些专业方向？

---

## 4. 兜底话术

| 意外 | 怎么说 / 怎么做 |
|---|---|
| 模型响应慢 | "这里走的是云端大模型，首次调用稍慢一点。"——继续讲检索过程条与来源面板，不要干等 |
| 回答里没有 `[N]` 标记 | 切换话题到来源面板："引用与否在右侧面板有明确标注，模型没引用的条目不会被标绿" |
| 网络中断报错 | 别解释网络，直接说："这类异常会进问答记录并标为报错，便于事后排查"——顺手把它变成治理能力的展示 |
| 问到资料外的内容 | **这是加分项，不是事故**：正好讲"资料不足时明确说明、不凭常识编造" |
| 时间不够 | 砍「治理与运维」里的操作日志页；砍「上传小文件」现场演示（改展示已上传的记录） |
| 时间有富余 | 加讲 `检索评测报告.md`：15 题 × 4 配置的命中率实测，并**主动说明为什么混合检索的首条命中率反而低于纯向量**（关键词通道按最大值归一化，每题必得满分份额）——这种"自己发现并解释缺陷"的讲述比只报好数字更可信 |

---

## 5. 附：可引用的实测数字（都已跑过，可直接念）

- 语料：11 条资源、1816 条有效切片、2 个知识库；OCR 部分 338 页 / 35.4 万字 / 置信度 0.788 / 154 秒
- 单次问答实测：向量候选 12 条、关键词候选 12 条 → 融合 top 3~5；265~689 个流式块；端到端 1.96~3.3 秒；token 约 1100 + 600
- 检索评测（`检索评测报告.md`，数字可复现）：教材术语型 10 题，纯向量 Hit@1 **10/10**、混合 8/10、纯关键词 7/10；
  口语化 5 题 Hit@1：纯向量 3/5、混合 2/5、纯关键词 2/5；Hit@5 四组配置均为 4/5~5/5；
  嵌入服务不可用时可自动降级为关键词检索，逐题结果与纯关键词一致
- 前端优化：问答页首屏包从 1097 kB 降到 201 kB（gzip 367 → 77 kB）
