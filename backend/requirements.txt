# Web 框架
Django>=5.2,<6.0
django-ninja>=1.3
django-cors-headers>=4.4
uvicorn[standard]>=0.32

# 数据库 / 缓存
psycopg[binary]>=3.2
pgvector>=0.3.6
redis>=5.2
celery>=5.4

# 配置
python-dotenv>=1.0

# 模型调用
httpx>=0.27

# 文档解析
python-docx>=1.1
pypdf>=5.1
ebooklib>=0.18
beautifulsoup4>=4.12
lxml>=5.3
openpyxl>=3.1
markdown-it-py>=3.0

# 检索
rank-bm25>=0.2.2
# 说明：中文分词不引入 jieba（其 sdist 在本机 pip 解包阶段失败），
# 改用自研的字符 bigram 分词器，见 rag/tokenizer.py，零依赖。

# 开发辅助
django-extensions>=3.2
